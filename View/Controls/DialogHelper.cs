namespace SPTC_APP.View
{
    internal class DialogHelper
    {
        /*public bool tbInputValid(TextBox tbox, Label dialogMSG, int level)
        {
            string tboxText = tbox.Text;
            switch (level)
            {
                case 1:
                    if (string.IsNullOrEmpty(tboxText))
                    {
                        dialogMSG.Content = "is empty. This field is required.";
                    }
                    else if (tboxText.Length > 60)
                    {

                    }
                    else if (Regex.IsMatch(tboxText, @"[^a-zA-Z0-9\s\-\.,/]"))
                    {

                    }
                    break;
            }
        }*/
    }
}
